public class VariableApp {
    //Global variable for java, need to be inserted here(in class)
    public String  name       = "yuiseo" ; //for name, We attribut 'String' so we need to use double quo
    public int     age        = 25;
    public double  height     = 172.5 ;
    public boolean isMarriage = true;
    public char    gender     = 'm' ; //for gender, we attibute 'char' so we need to use singlequo

    public static void main(String[] args) {

        /*
        varaibles defined in here are 'local variables'
        which only can be used in this method block
        */
        String msg = " this is starting point of programming" ;
    }
}

//and this class doesn't have 'Main' which is starting point of java, so it can be comfiled, but never excuted